# 牙齿矫正综合平台 执行计划

> 编写日期：2026-09-07（v1.1 修订：api 更换为新版脚手架 Admin.Api；确定文件存储设计）
> 修订：2026-09-08（v1.2：医生与患者共用同一个小程序，医生日常操作在小程序内完成；doctor 网页端保留为后期复杂排牙计划专项；删除优先级与排期，本文档作为 coding plan 使用）
> 修订：2026-09-08（v1.3：确认账号设计——医生/患者共用 `user` 账号表，`doctor` 为 1:1 档案扩展表，见第 4 节）
> 修订：2026-09-13（v1.4：与 `doc/` 迁入设计文档（六次会议结论 + M0 设计）逐项比对后补充——新增医患绑定与疗程、自测迁移、消息中心、订单供应链等模块；沉淀会议已定业务规则（第 6 节）；扩充插件清单、微信前置事项、待拍板清单。保持「不排期」原则不变）
> 修订：2026-09-13（v1.5：同步 `doc/01_产品大纲.md` 更新——新增未成年人模式/家人绑定/定位授权等待定项与「换医生数据继承」问题（4.1、6.1、11.2）；微调套餐标注待定；保持无忧套餐定义调整；AI 模拟游客规则细化）
> 关联文档：`doc/README.md`（doc 目录导航）、`doc/09_设计文档与实现现状对照.md`（两套文档冲突裁决，**先读**）、`CLAUDE.md`（工程约定）
> 状态：**未开始执行**

---

## 0. 文档体系与适用边界

`doc/` 下另有一套从前身仓库 smilecheck-app2 迁入的设计文档（`00`–`09` 共 10 份 + `2026-07-11.txt`），整理自六次会议与商业计划书。它与本计划在业务理解上一致，但在技术实现上有多处冲突（ORM、主键、时间列、schema、组件库等），逐条对照见 `doc/09`。

### 0.1 冲突时以谁为准

| 主题 | 以谁为准 |
| --- | --- |
| 工程约定、目录结构、API 约定、实体写法 | `CLAUDE.md` |
| 功能模块清单、分期计划、外部系统对接 | 本文档 |
| 会议结论、业务规则来源、产品定义 | `doc/00_历次会议大纲.md`、`doc/01_产品大纲.md` |
| 技术选型 | `CLAUDE.md`（与迁入文档的差异逐条见 `doc/09` §3） |
| 数据模型 | 本文档第 4 节；`doc/07` 的 DDL **不执行**，降级为字段与索引参考清单（约束/索引设计逐条评估后搬进 FreeSql 实体注解） |
| 部署与运维操作 | `doc/06`（本仓库 `docker/` 只有配置没有手册；OSS 与 CDN 两节留到切云存储时用） |

### 0.2 三根时间轴，不要混

| 轴 | 出处 | 用途 |
| --- | --- | --- |
| 业务三步走：保持器切入 → 保持器+微调 → 正畸平台（2026-09 → 2028+） | `doc/02` | 商业口径，**不用于排开发** |
| 开发里程碑 M0–M4（2026-09 → 2027-03） | `doc/04` | 已过时，不作为排期依据；其 14 项插件清单、负责人与前置依赖可嫁接进第 8 节 |
| 分期开发计划：阶段 0–6 + 后期专项 | 本文档第 8 节 | **开发排期唯一依据** |

---

## 1. 项目概述

牙齿矫正综合类平台，包含一个接口服务和三个前端（doctor 延后启动）：

| 端 | 目录 | 技术栈 | 使用者 | 说明 |
|---|---|---|---|---|
| 小程序（患者/医生共用） | `app/` | UniApp（Vue3） | 患者、医生 | 同一小程序按角色区分：患者功能 + 医生日常操作（患者管理、打卡跟踪、资讯发布等）；目前仅微信小程序，后续同一套代码扩展 Android / iOS |
| 平台管理后台 | `backend/` | Vue3 + Ant Design Vue | 平台工作人员 | 内容审核、机构/医生/产品管理、运营配置 |
| 医生端网页后台 | `doctor/` | Vue3 + Ant Design Vue | 医生 | **项目保留、延后启动**：日常操作走小程序，此端专用于后期复杂排牙计划（排牙方案设计与调整）等网页端操作 |
| 后台接口服务 | `api/` | ASP.NET Core 10（net10.0）+ FreeSql + PostgreSQL + Redis | — | 纯 Web API 无 UI；定时任务使用 FreeScheduler |

### 1.1 商业主线（会议口径，详见 `doc/02`）

第一步以透明保持器切入（存量正畸结束用户、经现有医生推广）→ 第二步加轻微反弹微调（Mini/Standard/Pro 套餐）与服务套餐 → 第三步做隐形矫治全流程与自有 OEM（目标 200 元/副，主攻下沉市场）。所有模块最终托举「医生与患者在平台上闭环一个 case」。

### 1.2 明确不做（会议已定，开发中不要顺手做）

- 不做预约系统；不做远程会诊/视频问诊（医患视频用微信）。
- 不做第二套医生 App；医生高级功能走网页端。
- 不自建商城 SKU/库存/物流/客服（接有赞类 SaaS）。
- 不做打卡防作弊（相信用户）。
- 不以「AI 诊断 / 在线治疗」为对外口径；不使用个人 IP。
- 初期商城不上正畸类产品。

外部系统（本期只做对接边界，不实现内部逻辑）：

- **AI 专业系统**（外部）：AI 模拟与专业分析报告由专业人员训练，本平台**预留接口**。
- **第三方商城系统**（外部）：商城由第三方支持，本平台只**拉取商品列表展示**。
- **微信开放能力**：小程序登录（jscode2session，已有雏形）、订阅消息（打卡/更换提醒）。

---

## 2. 总体架构

```
患者/医生小程序(app)   平台后台(backend)   医生端网页(doctor)
        \             |               /
         \  HTTPS：统一 REST API（Response<T> 包裹 + 请求签名 + Redis 会话）
          ▼
   api 接口服务（ASP.NET Core 10 + FreeSql + PostgreSQL + Redis + FreeScheduler）
          |                        |
          ▼                        ▼
   AI 专业系统（回调预留）    第三方商城（商品拉取）
```

### 2.1 开发约定（摘自 CLAUDE.md，全期遵守）

1. `Admin.Models` 新增实体后，必须同步在 `SystemService.InitSystemAsync()` 增加 `_fsql.CodeFirst.SyncStructure<T>()` 及默认数据。
2. 建表时根据字段业务属性（查询、排序、唯一性）用 `FreeSql.DataAnnotations.IndexAttribute` 加索引。
3. 三个前端全部走 i18n：**简体中文（默认）+ 英语**；api 用户可见文案通过 `Resources` 本地化。
4. API 统一 `Response<T>{code,msg,data}`；错误抛 `CodeException`；请求签名与会话机制沿用 `AdminCheck` / `AppCheck`。
5. 实体命名沿用现有风格：小写属性、`created_at/updated_at/deleted_at` 为 Unix 秒（long）、软删除。
6. 提醒、商城商品同步等定时任务统一用 FreeScheduler。
7. **留痕类列不建外键**：操作人/审核人/授予人等「谁操作了什么」的列一律不加外键约束——留痕要比账号活得久（见 `doc/08` §8.1）；多态引用（`target_type`+`target_id`）由服务层统一封装校验，删除目标时级联清理。
8. **写接口幂等**：创建 AI 任务、绑定医生、发布内容、创建订单、插件回调等写操作必须支持幂等（建议请求头 `App-Idempotency-Key`，服务端短窗去重返回首次结果）。

### 2.2 非功能目标（来自 `doc/03` §11，验收时对照）

| 指标 | 目标 |
| --- | --- |
| API P95 | 普通接口 < 300 ms；信息流 < 500 ms |
| AI 任务 | 正面模拟 ≤ 60 s 返回，失败率 < 5% |
| 可用性 | 核心链路（登录、打卡、私信、订单）99.5% |
| 容量 | 月活 10 万单实例可承载；扩到日活 100 万只需水平扩展（私信按 1:1 单点对聊设计） |
| 成本 | 约 1 元/用户/月 |
| 数据 | 每日备份并演练恢复；照片/口扫数据长期保留 |

---

## 3. 功能模块清单

| # | 模块 | 涉及端 | 外部依赖 |
|---|---|---|---|
| 0 | 账号/机构/权限基础 + 脚手架（api/backend/app，doctor 延后）+ 小程序存量代码迁移 | api + backend + app | 微信登录、短信 |
| 11 | 医患绑定与增长（医生二维码、邀请码、绑定规则、疗程）【新增】 | app + backend | 微信小程序码 |
| 10 | 自测问卷与偏好问卷（前身仓库功能迁移改造 + 注册偏好问卷）【新增】 | app + backend | — |
| 7 | 找机构、找医生、找产品 | 小程序浏览、后台发布管理 | — |
| 8 | 首页（框架 + 聚合） | 小程序 | 依赖各模块入口 |
| 3 | 牙套打卡 | 小程序（患者打卡、医生查看）+ 后台统计 | 订阅消息 |
| 4 | 复诊跟踪 | 小程序（患者 + 医生） | 订阅消息、商城入口 |
| 6 | 资讯（咨询）发布和浏览 + 积分 | app（患者/医生发布）+ backend（审核） | 对象存储（图视频）、内容安全机审 |
| 9 | 消息中心（1:1 私信 + 评论/关注/系统通知）【新增】 | app + backend | 订阅消息 |
| 12 | 保持器/牙套订单与供应链【新增】 | app + backend | 供应商设计软件（跳转 + webhook）、物流查询 |
| 5 | 美牙产品商城 | 小程序展示、后台同步配置 | 第三方商城 SaaS |
| 1 | 用户 AI 自测（矫正模拟 / 贴面模拟） | 小程序 | AI 专业系统 |
| 2 | 专业 AI 方案（报告） | 小程序（后期复杂排牙操作走 doctor 网页端） | AI 专业系统 |

> 编号 1–8 沿用原需求文档（该文件已缺失，见 `doc/README.md`「缺失」节）；9–12 为 2026-09-13 比对 `doc/` 后新增。上表顺序即建议开发顺序，分阶段落地见第 8 节。AI 两模块本期只需打通"上传—任务—回调—展示"链路（可 mock）。

---

## 4. 数据模型规划（主要表）

实体统一放 `Admin.Models`，遵循第 2.1 节约定。已有的 `user`、`account/role/module`、`corp/corp_department/corp_account`（模型已存在，初始化被注释）优先启用复用。

**账号设计决策【已确认】：认证身份合一，角色差异下沉到档案表。**

- **医生与患者共用 `user` 账号表**：两者都走微信小程序登录，身份锚点是 `openid`/`unionid`（唯一索引保证一个微信人一行）。`user` 新增 `role` 列（新枚举 `UserRole`：Patient / Doctor）；现有 `type`（Client / WechatMP）语义为注册渠道，保留不动。
- **`doctor` 为 `user` 的 1:1 档案扩展表**：`user_id` 唯一关联，存放姓名、职称、专长、简介等医生专属信息，以及平台认证状态（待审 / 通过 / 驳回）；不承担登录职责。`doctor_case`、`doctor_corp` 挂 `doctor_id`。
- **权限判定**：医生工作台 = session 中 `role == Doctor` 且 `doctor` 档案已通过认证。
- **边界**：医生本人也是患者（戴牙套/购物）天然支持——同一行账号既有档案又可用患者功能，也为将来"一个账号多角色"留了演进空间；平台后台人员走 `account`，机构内身份复用 `corp_account`，均不并入 `user`。
- **专家角色**：中心化方案团队（出/审方案）为第二业务阶段引入，本期不建模。

使用 `doc/07`（DDL V1，39 张表）时只作**字段与索引的参考清单**：主键用 `Guid`、时间列用 Unix 秒 `long`、建表走 FreeSql CodeFirst；`07` 的 ULID / `TIMESTAMPTZ` / 手写 SQL / 多 schema 均不采用。其外键取舍（尤其留痕列不建外键的 6 条清单）与索引设计逐条评估后搬进实体注解。

### 4.1 疗程与医患关系【待拍板，阶段 2 前必须落定】

9 月 7 日会议结论：**疗程（course）是医患关系的唯一单位**——医患多对多，换医生等于结束当前疗程另开一条，历史数据保留。本仓库现状没有这个概念，医患关系隐含在 `aligner_plan` 里。不落定则以下场景无落点：

1. 患者换医生后，历史打卡与复诊记录归属谁；
2. 医生看板「我的患者」如何界定（看 `aligner_plan` 还是看绑定关系）；
3. 二维码绑定后、尚未开始矫治时，医患关系存在哪里；
4. 同一患者先后被两个医生服务时，私信会话挂在哪一层；
5. 患者二次矫正换医生时，新医生能否（经患者授权）继承上一医生的历史数据（2026-09-13 产品侧新增问题，方案 A/B 都需给出答案）。

两个候选方案：

- **方案 A（贴合会议结论）**：新增 `course` 关系表（患者、医生、状态机：咨询中/进行中/已结束/保持期），`aligner_plan`、`replace_plan`、私信会话挂 `course_id`；配套 `course_event` 留痕。
- **方案 B（实现更省）**：`aligner_plan` 加 `doctor_id`，接受「一次矫治计划即一段医患关系」的简化；二维码绑定态用轻量关系表表达。

无论 A/B，`qr_scene`（扫码场景：医生码/邀请码，短码见 5.3）与 `invitation`（邀请关系与裂变层级）两张表都需要。

### 4.2 表清单

| 模块 | 表 | 用途 |
|---|---|---|
| 账号 | `user`（新增 `role` 列）、`doctor`（档案） | 患者与医生共用账号表；医生档案（1:1 扩展，含认证状态） |
| 绑定与增长 | `qr_scene`、`invitation`、`course`（待拍板，见 4.1）、`course_event` | 扫码场景短码、两层裂变邀请与层级校验、疗程、疗程事件留痕 |
| 自测/问卷 | `selftest_template`、`selftest_question`、`selftest_record`、`selftest_report`、`user_preference` | 前身仓库自测功能迁入；注册偏好问卷复用同一问卷模型，答案生成用户标签用于推荐 |
| 基础 | `corp`（扩展）、`corp_photo`、`corp_review` | 机构信息、环境相册、用户评价 |
| 找医生 | `doctor_case`、`doctor_corp` | 医生案例（按模板：前后照必填/视频可选）、执业机构关联（挂在 `doctor` 档案下） |
| 找产品 | `brand`、`product`、`product_ref` | 供应商品牌、产品、产品—医生/机构关联展示 |
| 牙套打卡 | `aligner_plan`、`checkin_record`、`wear_stat` | 矫治计划（牙套副数/日程）、摘戴打卡、时长/连续性/规律性统计与评分 |
| 复诊跟踪 | `replace_plan`、`replace_record` | 更换周期（默认 14 天、医生可自定义）、更换记录+口内照 |
| 资讯/积分 | `post`、`post_media`、`comment`、`follow`（如需点赞/收藏补 `reaction`）、`points_account`、`points_ledger` | 帖子（图文/视频+文字）、评论、关注、积分账户（医生/患者区分 owner_type）、积分流水 |
| 消息 | `conversation`、`message`、`notification` | 1:1 会话、消息、系统通知与订阅消息投递记录 |
| 订单供应链 | `product_order`、`design_job`、`shipment`、`repurchase_reminder` | 保持器/牙套订单状态机、供应商设计任务、发货物流、复购提醒 |
| 商城 | `mall_product`、`cart_item` | 第三方商品同步缓存、购物车 |
| 首页 | `banner`、`news` | 优惠活动、新闻动态 |
| AI 预留 | `ai_task`（预留 `provider` 列）、`ai_material`、`ai_report` | 统一任务模型：素材、任务状态、报告存储；将来接第二家 AI 供应商不用改表 |
| 文件 | `file_registry`（建议新增，见 5.6） | 最小文件账本：file_key、业务类型、业务 id、大小、创建时间 |

> 详细字段设计在各阶段开发前出表设计稿评审后再落码。

---

## 5. 外部系统对接设计

### 5.0 插件化整合总则（会议已定：「平台是整合者」）

核心自持用户、疗程、照片/口扫、订单数据与业务流程；外部能力以插件接入、可替换可降级。三类契约（详细协议模板见 `doc/05` §7，其中回调验签、幂等、兜底轮询部分直接采用）：

- **A 类 异步任务 API**（AI 模拟、图像质检等）：我方建任务 → 签名调插件 → 插件回调（验签 + 按 `task_id` 幂等 + 结果转存我方）→ 客户端轮询；回调丢失时定时兜底查询；失败率超阈值自动停用入口并告警。
- **B 类 跳转 + Webhook**（供应商设计软件、商城 SaaS）：我方签发一次性短时 token（含回跳地址，患者用别名不传真名）→ web-view/桌面跳转 → 对方 webhook 回传状态（验签、以 `{来源}:{外部单号}:{事件}` 为幂等键）。
- **C 类 基础设施封装**（微信能力、短信、物流、后期 OSS/CDN）：统一接口封装，业务代码不直接依赖具体厂商 SDK。

外部插件清单（编号沿用 `doc/04` §8）：

| 编号 | 插件 | 提供方 | 类 | 计划阶段 | 降级 |
| --- | --- | --- | --- | --- | --- |
| P1 | AI 正面排齐模拟 | LEO 现成模型服务化 | A | 6 | 入口隐藏 |
| P2 | 拍摄姿态检测 | 端侧能力（已实现） | — | 0（随迁移） | 引导模式 |
| P3/P4 | 侧貌 VTO、贴面/美白模拟 | LEO 研发 | A | 后期专项 | 不展示 |
| P5 | 牙套/保持器设计 | 供应商（美立克/昆山） | B | 4 | 线下流程 |
| P6 | 口扫处理/封牙/分割 | LEO 自研 | 内部服务 | 4 起 | 供应商承接 |
| P7 | 商城 | 有赞类 SaaS | B | 5 | 入口关闭 |
| P8 | 视频存储分发（OSS+CDN+转码） | 云厂商 | C | 切云存储时 | 降码率 |
| P9 | 微信订阅消息/小程序码/登录 | 微信 | C | 0–2 | 短信 |
| P10 | 支付（商品微信支付；套餐支付宝周期扣款） | 微信/支付宝 | C | 随订单/套餐 | 线下收款 |
| P11 | 内容安全机审 | 微信内容安全接口或云厂商 | C | 3 | 人工审核 |
| P12 | 大模型文案/问答（DeepSeek 等） | 第三方 | A | 可选 | 关闭 |
| P13 | 物流查询 | 快递查询服务/供应商回传 | C | 4 | 手工录入 |
| P14 | 短信 | 云厂商 | C | 0 | — |

### 5.1 AI（模块 1、2 —— 均为预留接口）

统一"AI 任务"模型，本期保证链路可空跑（mock 实现）：

```
用户上传素材 → 创建 ai_task(pending) → 推送/暴露给 AI 专业系统
→ 外部回调写入结果（签名鉴权）→ task=done → 小程序展示
```

- **模块 1 用户 AI 自测**：拍照/上传 → 提交矫正模拟或贴面模拟任务 → 前后两张照片对比展示。接口：提交任务、查询任务、获取结果图。**已定取消评分制，改为前后对比**（评分主观难做准，用户自己看得出）；结果附免责声明 → 推相似真实案例 → 推医生（同城优先）；游客规则已细化（2026-09-13 产品口径）：正面模拟游客可试用，侧貌模拟需注册，保存结果需登录；每日次数走系统配置。
- **模块 2 专业 AI 方案**：批量上传面相照、口内照、CBCT、侧位片、牙齿 3D 模型 → 生成专业分析任务 → 展示分析报告（AI 或人工产出均走同一报告模型）。复杂排牙计划的 doctor 网页端操作为后期专项（见第 8 节）。
- 正面排齐模拟是首个真实插件（P1，LEO 现成模型约 30 秒出结果）；LEO 交付接口前用 mock 插件（返回原图 + 固定延迟）打通全链路，不阻塞开发。
- `ai_task` 预留 `provider` 列【建议，待拍板】：本期单一 AI 供给，将来接第二家供应商不改表。
- 对外提供回调接口，沿用签名鉴权；素材规格与回调协议需与 AI 团队约定（见第 11 节）。

### 5.2 第三方商城（模块 5）

- 方向已定：接**有赞类 SaaS**（对方管商品/库存/支付/物流/客服），小程序内展示与下单；平台自营选品，医生不能上架；初期不上正畸类产品；美白剂购买可关联打卡。
- FreeScheduler 定时任务拉取第三方商品列表 → 写入 `mall_product` 缓存表；后台可手动触发同步。
- 订单 webhook 回传，我方只存订单引用；购物车归属、页面承载方式（内嵌 H5 vs 跳转）、支付是否小程序内闭环见第 11 节。

### 5.3 微信生态

- **订阅消息模板**（需提前申请，在用户操作节点收集一次性订阅授权）：换套提醒（提前一天+当天）、打卡提醒（每日）、私信提醒、订单状态（设计完成/发货/签收）、报告/模拟完成。
- **小程序码**：`getUnlimited` 的 scene 上限 32 字符且仅支持数字、字母与部分符号 → 设计 **≤20 字符短码**（前缀 + Base62 序号，如医生码 `d3f8k2`、邀请码 `iA9x7q`），**不要把 Guid 塞进 scene**；落地页统一解析。
- **登录**：`jscode2session`（已有雏形）+ 手机号验证码绑定（短信）；`getPhoneNumber` 一键授权待公司主体认证后评估。
- **内容安全**：UGC 文本/图片机审（msgSecCheck/imgSecCheck 或云厂商），结果写审核状态。
- **域名白名单**：request / uploadFile / downloadFile 合法域名与 web-view 业务域名分别配置（均需 HTTPS）。

### 5.4 短信

验证码登录/绑定与系统通知；签名与模板报备约 1–2 个工作日；仅服务端调用。

### 5.5 保持器/牙套订单供应链（模块 12，新增）

- 订单状态机：需求登记 → 口扫建档（线下完成，STL/PLY 上传存档）→ 供应商设计（B 类跳转）→ 专家/医生确认 → 生产 → 发货物流 → 签收 → 复购提醒；医患均可在小程序查看进展。
- 供应商 webhook 事件枚举：`design.started/completed/rejected`、`production.started/completed`、`shipped`、`delivered`、`cancelled`。
- 设计费 50–100 元/次由供应商侧计费，按次计量记账（可归到订单）。

### 5.6 文件存储（本地存储 + 签名地址直传/下载）【已确认】

存储先用本地磁盘，但接口按**预签名**语义设计，未来可平滑切换 OSS：

- **上传**：客户端先调接口申请签名上传地址（携带文件名/类型/大小/业务场景）→ 获得 `upload_url` + `file_key` → 向 `upload_url` **直传**文件 → 业务侧只落库 `file_key`。
- **下载**：客户端申请签名下载地址（或业务接口直接返回带签名的 `download_url`）→ **时效内**可下载，过期失效。
- **实现**：api 提供签发接口；上传/下载端点用 HMAC（复用 `EncryptorService` 密钥体系）校验签名与有效期后读写本地磁盘。切换 OSS 时仅替换读写实现为 OSS 预签名，接口语义不变，前端无需改造。
- **文件登记表（建议新增，待拍板）**：签发接口写一行最小账本（file_key、业务类型、业务 id、大小、创建时间）——本地磁盘阶段即可反查孤儿文件，切 OSS 后的清理也依赖它。
- **全库统一只存 `file_key` 字符串一套机制**，不要混用 file_id / 反向多态等多套关联（`doc/08` §8.2 的教训）。
- **敏感文件原则**：自测照片、复诊照、口扫模型、医生认证材料私有 + 短时签名访问（默认 10 分钟）+ 查看留痕；目录规范参考 `doc/06` §6.2。
- 待开发中确定的小参数：签名有效期时长、单文件大小上限、大文件（CBCT/3D 模型）是否需要分片。

---

## 6. 业务规则基线（会议已定，编码按此执行）

细则与 PM 待产出项见 `doc/01`；此处只收对开发有直接约束的规则。与 `doc/05`/`doc/07` 冲突时以本节为准。

### 6.1 注册与登录

- 微信小程序静默登录（openid 锚点）+ 手机号验证码绑定（已定首选；`getPhoneNumber` 一键授权待公司主体认证后评估）。
- 注册时偏好问卷十几题 → 生成用户标签，用于内容/医生/方案推荐。
- 未成年人（儿童早矫场景）需监护人提示；自测报告自动保存，可在「我的」管理删除。
- 产品侧新增待定需求（2026-09-13）：未成年人模式（限制购买行为）、家人绑定模式（家长为注册人，1 个孩子绑定 1 个家长）、同一账号多设备登录的数据可见性、授权阶段是否获取定位城市/地区——均未定案，见第 11 节。

### 6.2 医患绑定（bind 规则，服务层必须实现）

1. scene 短码解析类型：`doctor`（医生码）/ `invite`（患者邀请码）。
2. 医生码：目标医生须认证通过，否则拒绝。
3. 邀请码：取邀请人当前有效疗程的医生为挂靠医生；邀请人无医生则拒绝。
4. 裂变层级：医生 → 患者 → 患者，**最多两层**；邀请人自身已是第二层被邀请者时本次为第三层 → 拒绝。
5. 不能扫自己的码。
6. 与该医生已有进行中疗程 → 幂等返回既有疗程（不报错）；与其他医生有进行中疗程 → 提示用户决定（结束当前再绑或仅关注）。
7. 所有绑定写事件表留痕；绑定成功后医生收到通知。

### 6.3 打卡与评分

- 摘/戴双打卡，底部中央加号一键操作，系统按上一事件类型自动判断本次是戴上还是摘下。
- **评分 = 总时长 60% + 连续性 30% + 均衡性 10%**（权重可配置）；医患都能看图表。
- 低于阈值触发干预：阈值与动作待定（会上提过 70/80 分）。
- 不做防作弊。

### 6.4 复诊监控

- 默认 14 天换一副（医嘱可改）；换套提醒**提前一天 + 当天**（订阅消息 + 消息中心）。
- 换套时引导拍照上传（旧套/新套状态），医患可见；平台侧初期人工核对，**AI 对比（脱套/不贴合标签）为后期专项**。
- 宣传口径全部用 AI，初期后台人工。

### 6.5 裂变与积分

- 积分来源：邀请注册、发布牙套日记/内容、完成打卡等非消费行为；用途：周边产品、优惠券（向门诊争取）、保持器。
- 「患者合伙人」消费分成模式待法务评审，**暂不进产品**。
- 医生积分 = 推广患者数 + 患者平台消费额 + 内容数量质量 → 排名与广告位（排名算法后期）。

### 6.6 内容

- 小红书式单一信息流：科普/案例/日记/互助统一帖子模型；按作者类型（医生/患者/官方）过滤；推荐先按规则（偏好标签），AI 多维推荐后期。
- 医生案例按模板提交（前后照必填、视频可选、文字说明）；医生有个人主页、可被关注，医生即大 V。
- 视频自托管（本期本地存储 + 预签名，后期 OSS+CDN），不外链第三方。
- 前期运营小号活跃社区；上线前内容团队灌入官方科普与医生案例，避免信息流空壳。

### 6.7 消息与沟通

- 站内 1:1 私信（医患）+ 帖子评论 + 医生主页留言 + 系统通知；配微信订阅消息触达；医生端支持集中回复。
- 不引导加医生微信；不做视频会诊。

### 6.8 合规口径（文案硬约束）

| 可以用 | 禁止用 |
| --- | --- |
| 牙齿健康自测、AI 辅助模拟、正畸后保持管理、保持器复购、口扫建档、佩戴提醒、反弹监控、轻微微调适合度评估、合作机构服务 | AI 诊断/AI 确诊、在线治疗、医生确诊、AI 自动治疗方案、保证不反弹/排齐/变美、无需面诊/无需医生、完全替代隐形正畸、个人 IP 相关表述 |

- AI 模拟/报告统一附免责：AI 生成，仅供参考，以医生评审为准，不作为签约依据。
- 用户侧文件：用户协议、隐私政策、照片/口扫数据授权书、自测免责声明、医疗服务边界说明、未成年人提示；版本化管理，协议更新后用户下次启动需重新同意。

---

## 7. 小程序信息架构（app）

### 7.1 底部 tab（建议，待产品确认）

```
首页 | 发现(信息流) | [+ 打卡] | 消息 | 我的
```

- 购物车不占 tab 位：商城入口放首页宫格 +「我的-订单」。
- tabBar 用微信标准 5 项还是自定义中央凸起按钮待确认（建议先用标准，见第 11 节）。
- 医生身份在「我的」追加：我的患者、我的二维码、我的内容、方案设计、数据看板。

### 7.2 分包规划（主包 ≤ 2MB）

| 包 | 内容 | 计划阶段 |
| --- | --- | --- |
| 主包 | 5 个 tab 页 + 登录/协议 + 扫码落地页 | 0 |
| pkg-assess | 自测问卷/拍照引导/相机（姿态检测）/报告 | 0（迁移） |
| pkg-relation | 找医生/医生主页/我的疗程/绑定确认 | 1 |
| pkg-care | 打卡历史/复诊监控/换套任务 | 2 |
| pkg-doctor | 患者列表/看板/二维码/发布案例 | 2 |
| pkg-webview | 通用 web-view 承载页（供应商/商城 H5） | 4 |
| pkg-trade | 订单/物流/商城入口 | 4–5 |
| pkg-sim | AI 模拟上传/结果对比/记录 | 6（占位可提前） |

### 7.3 存量代码迁移

前身仓库（uni-app Vue3 + TypeScript + Pinia）已有 13 个页面（自测/拍照引导/相机/报告/科普/登录/协议）与姿态检测降级链（VisionKit → wx.faceDetect → 引导模式），本仓库 `app/` 目录目前为空。**按 `doc/05` §8.5 迁移清单逐文件执行**（保留/改造/废弃已逐条标注）。迁移时同步处理：TypeScript 升级 5.x、签名密钥改构建期注入（不硬编码）、tabBar 索引调整、报告页弱化评分展示、路径改分包路径。

---

## 8. 分期开发计划

### 阶段 0：基础设施与账号体系

**目标**：api / backend / app 脚手架可运行、可登录、可部署（doctor 延后）；权限与机构模型就绪；医患绑定链路可用。

- **api**（新版脚手架已就绪：`Admin.Api` 纯 Web API，net10.0，控制器/过滤器/DI/本地化均已启用）
  - 启用并完善 `corp` 系列实体；按第 4 节账号设计落地：`user` 新增 `role` 列（患者/医生共用账号表）、新建 `doctor` 档案表（含认证状态）；区分后台人员 / 医生 / 患者三类角色，医生可登录小程序进入医生工作台。
  - 微信小程序登录联调（`wx-mp-login` 已有雏形；顺手修复 `AppCheckAttribute` 误用 `AdminSessionService` 校验会话的问题，应为 `AppSessionService`）；短信验证码服务接入（P14）。
  - 医患绑定接口（bind 规则见 6.2）与 `qr_scene`/`invitation` 表；疗程表落地视 4.1 拍板结果（若暂缓，绑定关系需有过渡落点）。
  - 文件签名直传/下载服务（本地存储 + 预签名接口，见 5.6）。
  - 默认请求文化改为 zh-Hans（当前 `Program.cs` 默认 en-US，与"默认简体中文"需求不符）；`Resources` 资源补全中英两种语言。
- **backend**：Vue3 + Vite + TS + Pinia + Vue Router + vue-i18n + Ant Design Vue 脚手架；登录、布局、菜单权限（对接 module/role）；请求封装（签名/会话头）；用户/医生管理与认证审核最小版。
- **app**：UniApp（Vue3）脚手架 + **存量代码迁移**（见 7.3）；分包结构落地（见 7.2）；微信登录（区分患者/医生角色，医生登录后进入医生工作台）+ 手机号验证码绑定；偏好问卷页；扫码落地页与 scene 短码解析；i18n；请求封装（App 会话 + 签名 + 幂等头）。
- **微信平台**：域名白名单配置、订阅消息模板申请、小程序码短码规则定稿（见第 9 节）。
- **docker**：nginx 增加 backend 静态站点与 api 反代配置（doctor 站点待排牙专项启动时补充）；核对 dockerfile 产物名（`Admin.Api`）。
- **交付物**：api / backend / app 空壳可登录（患者、医生角色）；医生可生成二维码、患者扫码可完成绑定；部署脚本更新。

### 阶段 1：找机构 / 找医生 / 找产品 + 首页框架

**目标**：小程序内容骨架可用，后台可维护基础数据。

- **api**：机构（资质/地点/环境/服务/评价）、医生（简介/案例/所属机构）、产品（品牌/关联医生与机构）的 CRUD 与查询接口；评价接口；首页聚合接口（banner、news、各模块入口）。
- **backend**：机构/医生/品牌/产品管理页；banner、新闻管理；评价审核。
- **app**：找机构（大众点评式详情页）、找医生（列表 + 筛选 + 医生主页含案例与二维码）、找产品列表与详情；首页框架（打卡入口占位、更换提醒占位、优惠活动、新闻动态、关注动态占位、购物车与我的入口占位）；自测问卷模块随迁移代码恢复可用（弱化评分展示，标签用于推荐）。
- **交付物**：小程序可浏览机构/医生/产品；后台可维护。

### 阶段 2：牙套打卡 + 复诊跟踪

**目标**：患者核心闭环上线，医生可在小程序内跟踪。

- **api**：`aligner_plan` 矫治计划管理；摘/戴打卡接口（按上一事件自动判断戴/摘）；佩戴时长、连续天数、均衡性统计与加权评分（**权重已定 60/30/10，实现为可配置**，见 6.3）；`replace_plan` 更换周期（默认 14 天、医生可改）；更换记录+口内照上传；FreeScheduler 提醒任务（**提前一天 + 当天**）+ 订阅消息推送。
- **依赖**：4.1 疗程建模拍板（「我的患者」界定、归属查询依赖它）；微信订阅消息模板申请通过。
- **app（患者）**：打卡页（摘/戴一键打卡）、打卡日历与统计展示、评分与更换提醒展示、复诊跟踪页（更换拍照上传）、**口内拍照神器购买入口**（跳转商城模块）。
- **app（医生角色）**：患者列表、打卡数据看板（时长/连续性/均衡性/评分）、更换计划调整、换套照片查看与反馈。
- **backend**：打卡/复诊数据统计报表。

### 阶段 3：资讯发布与浏览 + 积分 + 消息中心

**目标**：类小红书内容社区上线，积分账本就绪（用途预留），医患沟通通道打通。

- **api**：帖子发布（图片/视频 + 文字）、浏览/信息流、评论、关注（医生↔患者互关，如需点赞/收藏补 `reaction`）；积分规则（发布文字得积分）与 `points_account/points_ledger` 账本（医生/患者区分，作用后续备用）；首页"关注动态"聚合；内容机审接入（P11，时机视拍板）。
- **消息**：`conversation/message/notification` 三表与接口；私信发起规则（细则待定）；未读计数；订阅消息触达；评论/关注/系统通知聚合；医生集中回复列表。
- **app（医生角色）**：医生在小程序内发布/浏览资讯、查看粉丝与评论、集中回复私信。
- **app（患者）**：资讯信息流、详情、发布、评论、关注页；消息 tab（私信/互动通知/系统通知，未读角标）。
- **backend**：工作人员发布入口、内容审核（先发后审/先审后发待确认）；私信按投诉工单调阅并留痕（不可随手读正文）；通知模板配置。
- **依赖**：阶段 0 的文件签名直传服务就绪（图片/视频走 5.6 设计）。

### 阶段 4：保持器/牙套订单与供应链【新增阶段】

**目标**：第一现金流域跑通「登记 → 口扫 → 设计 → 确认 → 生产 → 物流 → 复购提醒」全链路。

- **api**：`product_order` 订单状态机；设计任务 B 类跳转 token（患者别名、短期有效、可在线校验，见 5.0）；供应商 webhook（验签 + 幂等 + 事件枚举见 5.5）；物流同步（P13）；复购提醒（FreeScheduler）；口扫模型（STL/PLY）上传存档（走 5.6 通道）。
- **app**：需求登记、订单详情与物流、复购提醒；医生角色：发起订单、方案设计跳转入口。
- **backend**：订单查看、供应商对接配置、设计费计量对账。
- **依赖**：供应商接入方式与计费谈判（P5，商务，需提前启动）；P6 自研封牙/分割模块作为后备。

### 阶段 5：美牙产品商城对接（原阶段 4，依赖第三方就绪）

**目标**：商品展示与购买跳转可用。

- **api**：第三方商城商品拉取与定时同步（FreeScheduler）、商品列表/详情接口、购物车接口、订单 webhook 引用回传。
- **backend**：同步配置（手动触发、同步日志）。
- **app**：商品列表/详情、购物车、"我的"完善；阶段 2 的拍照神器入口落地到此。
- **依赖**：第三方商城 API 文档与联调账号（**外部依赖，需提前启动商务对接**）。

### 阶段 6：AI 接口预留与联调（原阶段 5，依赖 AI 团队协议）

**目标**：模块 1、2 的"上传—任务—回调—展示"链路可演示（AI 结果可用 mock 图/报告）。

- **api**：`ai_task/ai_material/ai_report` 落库（`ai_task` 预留 `provider` 列，视拍板）；小程序任务提交/状态查询/结果获取接口；对 AI 专业系统的任务推送与回调接口（签名鉴权，契约模板用 `doc/05` §7.2）；大文件（CBCT/3D 模型）走 5.6 签名直传通道，必要时评估分片上传；AI 模拟每日次数限制走系统配置。
- **插件**：正面排齐模拟（P1）先 mock 打通，再接 LEO 现成模型服务化。
- **app（患者）**：AI 自测页（拍照/上传、前后对比展示、免责声明、分享）、专业方案页（素材上传、报告展示）。
- **app（医生角色）**：医生查看与管理自己患者的方案任务与报告。
- **backend**：方案任务列表、报告查看、人工出报告的录入入口（模块 2 支持人工）。
- **依赖**：与 AI 团队确认回调协议、素材规格。

### 后期专项（暂不排期）

- **doctor 网页端复杂排牙计划**：doctor 项目保留但延后启动，日常医生操作均在小程序内完成。doctor 网页端专用于复杂排牙计划（如基于牙齿 3D 模型的排牙方案设计、调整与确认），待 AI 排牙能力与协议明确后单独立项，届时补充脚手架（与 backend 同栈：Vue3 + Ant Design Vue）、表设计与接口。
- **服务套餐与微调**：保持无忧（口径已调整：侧重保持器品牌/材料/厚度/种类升级，不再提「损坏免费换、反弹免费微调」）/牙周护理/家庭套餐（年付 + 支付宝周期扣款）、微调套餐（Mini 3–5 副 / Standard 6–10 副 / Pro 10–15 副，**产品侧标注待定**）——第二业务阶段。
- **AI 能力扩展**：侧貌 VTO / 贴面 DSD / 美白模拟并入模拟入口；复诊监控 AI 对比（脱套/不贴合标签）与医生处理工作台。
- **增长深化**：推荐算法（偏好 + 行为多维匹配）、话题/标签体系、医生排名与广告位、奖励目录。

---

## 9. 微信平台与合规前置（申请周期长，尽早启动）

| 事项 | 要点 | 周期/约束 |
| --- | --- | --- |
| ICP 备案 | 需公司主体 + 大陆服务器（包月 ≥3 个月） | **15–20 个工作日，上线硬前置**；备案期间 test 环境用 IP + 端口直连 |
| 公安联网备案 | 备案通过后 30 日内办理；页面底部展示备案号 | 上线检查项 |
| 小程序主体 | 公司营业执照注册；正式 AppID 到手前用测试号开发，体验版/正式版不允许 IP 与非 HTTPS | 阻塞体验版与模板申请 |
| 类目与资质 | 医疗健康类目资质材料；商城若上二类器械需《第二类医疗器械经营备案》——**建议先只上日化品，器械资质后期补** | 避免卡首次提审 |
| 订阅消息模板 | 按 5.3 清单提前申请 | 模板审核需时间 |
| 域名白名单 | request / uploadFile / downloadFile 合法域名 + web-view 业务域名（HTTPS、校验文件） | 与部署域名规划同步 |
| 合规文案包 | 协议、隐私、数据授权、AI 免责、未成年人提示、禁用词自检（见 6.8） | 上线前全部就位 |

---

## 10. 部署与运维

操作手册用 `doc/06_阿里云基础设施部署手册.md`（本仓库 `docker/` 目前只有配置没有手册），其中与仓库现状的差异按 `doc/09` §3 修正：

- **拓扑**：单台 ECS（杭州，Ubuntu 24.04，4C8G + 独立数据盘挂 `/data`）+ Docker Compose（nginx + api + postgres + redis）+ ACR 镜像仓库 + CI 一键发布（含健康检查失败自动回滚）；dev / test / prod 三环境，test 与 prod 同机分容器。
- **改造点**：`doc/06` 的 `.env`、compose、nginx 配置可直接改造使用，但数据库名/镜像名/路径需对齐仓库实际（数据库 `admin`、镜像与部署路径沿用项见 CLAUDE.md 提示的 `/opt/smilecheck/` 核对项）。
- **OSS 双桶与 CDN 两节本期不实施**（文件存储走 5.6 本地磁盘 + 预签名），切云存储时启用。
- **备份**：每日 `pg_dump` + 异地存储 + 恢复演练（M0 内必须做一次并记录耗时）；监控告警、上线检查清单、应急预案见 `doc/06` §8–§11。
- **迁移与发布分离**：表结构只在 `--init` 时同步（**人工执行，见 CLAUDE.md 约定**），禁止应用启动时自动迁移——与 `doc/06` 的数据库迁移策略一致。

---

## 11. 风险与待拍板清单

### 11.1 已确认

- 文件存储：本地磁盘 + 签名地址直传/下载（见 5.6）。
- api 脚手架：新版 `Admin.Api`（net10.0 纯 Web API，无 Blazor）。
- 账号体系：医生/患者共用 `user` 账号表（`role` 列区分），`doctor` 为 1:1 档案扩展表（见第 4 节）。
- `doc/07` DDL 不执行，降级为字段与索引参考清单（见 0.1）。
- 阶段划分以本文档第 8 节为准（`doc/09` §8 建议，已采纳）。
- 业务口径（会议已定）：微信小程序承载患者/医生两角色；内容单一信息流、视频自托管；商城走有赞类 SaaS 方向；打卡评分权重 60/30/10；裂变最多两层且挂靠医生；不做预约/会诊/防作弊/第二套 App（详见第 6 节）。

### 11.2 待拍板（按紧急程度排序，结论回写本文档）

**架构与技术（周会过一遍）**

1. **疗程建模**（4.1 方案 A/B）——不定则打卡/复诊归属查询与「我的患者」界定写不出来，**阶段 2 前必须落定**；含 2026-09-13 新增问题：二次矫正换医生时历史数据能否经患者授权继承（见 4.1 第 5 条）。
2. 会话 60 分钟滑动是否够用（打卡类日活应用偏短）：延长有效期 or 加静默续期。
3. 内容审核：先审后发 vs 先发后审（帖子与评论可能策略相反）；机审插件（P11）是否提前到阶段 3。
4. 文件登记表 `file_registry` 是否引入（见 5.6，建议加）。
5. `ai_task.provider` 列是否预留（见 5.1，建议留）。

**产品规则（PM 推进，来源 `doc/00` §5、`doc/01` §8）**

6. 登录最终方案：手机号验证码 vs 微信一键授权；何时索取手机号。
7. 私信发起规则（谁可以给谁发）、集中回复体验。
8. 打卡低分阈值与干预动作、漏打卡补录规则、多副牙套切换处理。
9. 复诊拍照规范（角度/数量）与 AI 对比结果的呈现方式。
10. 偏好问卷与自测问卷题库；报告「微笑类型」是否保留。
11. AI 模拟游客规则细则（方向已定：正面可试用、侧貌需注册）与每日次数上限、分享卡片样式。
12. tabBar 标准五项 vs 自定义中央凸起按钮（见 7.1）。
13. 评价是否可匿名；机构数据来源与录入方式。
14. 未成年人模式（限制购买行为）与家人绑定模式（家长为注册人、1 个孩子绑定 1 个家长）的产品定义与账号模型影响（2026-09-13 新增）。
15. 同一账号多设备登录：会话策略与数据可见性（2026-09-13 新增）。
16. 授权阶段是否获取定位城市/地区，影响同城推荐与找医生排序（2026-09-13 新增）。

**商务与资质依赖（需提前启动）**

17. 第三方商城具体服务商、API 能力范围、购物车归属（平台侧 or 商城侧）、支付是否在小程序内闭环。
18. 供应商（美立克/昆山）接入方式与计费口径、保持器订单状态机细节。
19. AI 对接协议：回调鉴权方式、素材规格（照片拍摄要求、CBCT 导出格式、3D 模型文件格式）、任务时效。
20. 微信合规：医疗健康类目及资质、订阅消息模板审核、内容与活动文案的医疗广告合规。
21. 品牌名与商标检索（「牙套之家」等存在已被注册风险）、小程序名与域名。
22. 积分规则：医生/患者是否分账分规则、发放细则、防刷机制、合伙人分成合规结论。
23. 多租户粒度：医生相关数据按机构隔离（复用 `corp_account`），连锁机构多院区是否需要 `corp_department` 启用。
24. 提醒策略：更换提醒的触发时间点、免打扰时段、患者关闭提醒的处理。

---

## 12. 质量与验收

- 每阶段结束做前后端联调（api + backend + app）+ 中英双语文案检查 + docker 部署验证。
- 目前无测试项目；自阶段 0 起按「测试底线」补齐（`doc/05` §10.4）：
  - **单元测试**：绑定与裂变层级校验、疗程状态机、签名与幂等、打卡评分算法、积分账本、商城同步。
  - **集成测试**：Testcontainers 起 PostgreSQL + Redis，覆盖「登录 → 扫码绑定 → 发帖 → 任务提交」类全链路。
  - **真机回归**：每阶段一次（微信开发者工具 + iOS/Android 真机），清单由产品维护。
- 接口设计基线：统一响应包与签名约定；错误码与既有 `CodeException` 取值对齐（**签名不符是 402**），业务错误码分段建议（认证 1000–、关系与疗程 1100–、内容 1200–、自测与模拟 1300–、文件 1400–、插件 1500–、订单 1600–）见 `doc/05` §5.3；列表接口建议统一游标分页；写接口幂等见第 2.1 节第 8 条。
- 所有新表执行 CLAUDE.md「新表检查清单」，并追加：状态列用枚举 + 中文文档注释；留痕列不建外键；多态引用由服务层统一校验（第 2.1 节第 7 条）。
- 产品北极星指标（验收参考）：**绑定了医生且 30 天内有打卡或互动的活跃患者数**（其余指标体系见 `doc/01` §7）。
